<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="AllTemplatesCS.Pages.ListViewPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:AllTemplatesCS.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="400"
      Title="ListViewPage">
    <StackPanel Margin="20">
        <ListView x:Name="SampleListView" Height="200" Width="350">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Name" Width="120" DisplayMemberBinding="{Binding Name}"/>
                    <GridViewColumn Header="Type" Width="80" DisplayMemberBinding="{Binding Type}"/>
                    <GridViewColumn Header="Size" Width="60" DisplayMemberBinding="{Binding Size}"/>
                    <GridViewColumn Header="Modified" Width="80" DisplayMemberBinding="{Binding Modified}"/>
                </GridView>
            </ListView.View>
        </ListView>
        
        <TextBlock Text="ListView with GridView columns showing file-like data" 
                   FontStyle="Italic" FontSize="10" Margin="5" HorizontalAlignment="Center"/>
    </StackPanel>
</Page>
